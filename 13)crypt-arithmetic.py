from re import sub
def solve(q):
    n=(i for i in q if i.isalpha()).next()
    except stopIteration:
        returnq if eval(su(r'([^0-9]+)',r'\1\2',q))else false
    else:
        for i in (str(i) for i in range(10) if str(i)- not in q):
            r=solve(q.replace(n,str(i)))
            if r:
                return r
            return false
    if_name_=="_main_":
        query="ABCDE*A==EEEEEE"
        r=solve(query)
        print r if else "NO SOLUTION FOUND."
